<Window x:Class="InfoDropletsClient.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"  
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:InfoDropletsClient"
        xmlns:map="clr-namespace:GMap.NET.WindowsPresentation;assembly=GMap.NET.WindowsPresentation"
        xmlns:vms="clr-namespace:InfoDroplets.Client.ViewModels"
        mc:Ignorable="d"
        Title="MainWindow" Height="675" Width="1200">
    <Window.DataContext>
        <vms:MainWindowViewModel/>
    </Window.DataContext>

        <Window.Resources>
        <Style TargetType="Button" x:Key="RCButton">
            <Setter Property="Height" Value="25"/>
            <Setter Property="Background" Value="#4CAF50"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#388E3C"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
    </Window.Resources>
    
    <Grid Background="#F2F2F2">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <!-- Top toolbar row -->
            <RowDefinition Height="1*"/>
            <!-- Main content row -->
            <RowDefinition Height="8*"/>
            <!-- Status row -->
            <RowDefinition Height="0.4*"/>
        </Grid.RowDefinitions>

        <Border x:Name="NavbarArea" Background="#E6E6E6" Margin="7" CornerRadius="10" Grid.Column="0" Grid.Row="0" Grid.ColumnSpan="2">
            <WrapPanel Margin="3">
                <StackPanel Grid.Row="0" VerticalAlignment="Center">
                    <Label x:Name="lb_SerialPort" Content="0{Binding SerialWrapper.SelectedSerialPort}" ContentStringFormat="Serial Port: {0}"/>
                    <Label x:Name="lb_BaudRate" Content="0{Binding SerialWrapper.SelectedBaudRate}" ContentStringFormat="BaudRate: {0}"/>
                </StackPanel>
                <Button x:Name="bt_ChangeSerialSettings" Content="Change" Width="70" Command="{Binding ChangeSerialPortSettingsCommand}"/>
            </WrapPanel>
        </Border>

        <Border x:Name="MapArea" Background="#E0E0E0" Margin="7" CornerRadius="10" Grid.Column="0" Grid.Row="1" Padding="3">
            <map:GMapControl x:Name="myMap" CenterPosition="{Binding MapCenterPos}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" ClipToBounds="True"/>
        </Border>

        <Border x:Name="DetailsArea" Background="#D9D9D9" Margin="7" CornerRadius="10" Padding="10" Grid.Column="1" Grid.Row="1">
            <StackPanel>
                <TextBlock Text="🎈 Droplet #1" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>

                <TextBlock Text="Altitude:" FontWeight="Bold"/>
                <TextBlock x:Name="tb_altitude" Text="{Binding SelectedDroplet.LastData.Elevation, StringFormat={}{0} m}" Margin="0,0,0,10"/>
                <TextBlock Text="Speed:" FontWeight="Bold"/>
                <TextBlock x:Name="tb_speed" Text="{Binding SelectedDroplet.SpeedKmH, StringFormat={}{0} km/h}" Margin="0,0,0,10"/>

                <TextBlock Text="Latitude:" FontWeight="Bold"/>
                <TextBlock x:Name="tb_latitude" Text="{Binding SelectedDroplet.LastData.Latitude}" Margin="0,0,0,10"/>

                <TextBlock Text="Longitude:" FontWeight="Bold"/>
                <TextBlock x:Name="tb_longitude" Text="{Binding SelectedDroplet.LastData.Longitude}" Margin="0,0,0,10"/>

                <TextBlock Text="Status:" FontWeight="Bold"/>
                <TextBlock x:Name="tb_status" Text="{Binding SelectedDroplet.MovementStatus}" Margin="0,0,0,10"/>

                <TextBlock Text="Distance:" FontWeight="Bold"/>
                <TextBlock x:Name="tb_distance" Text="{Binding SelectedDroplet.SpeedKmH, StringFormat={}{0} km}" Margin="0,0,0,10"/>

                <TextBlock Text="Last update:" FontWeight="Bold"/>
                <TextBlock x:Name="tb_lastUpdate" Text="{Binding SelectedDroplet.LastUpdated}" Foreground="Green" Margin="0,0,0,10"/>

                <Grid Background="DarkGray">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1*"/>
                        <ColumnDefinition Width="1*"/>
                        <ColumnDefinition Width="1*"/>
                    </Grid.ColumnDefinitions>
                    
                    <Button Content="File version" Style="{StaticResource RCButton}" Margin="4" Grid.Column="0"/>
                    <Button Content="Ping" Style="{StaticResource RCButton}" Margin="4" Grid.Column="1">
                        
                    </Button>
                    <Button Content="Restart" Style="{StaticResource RCButton}" Margin="4" Grid.Column="2"/>
                </Grid>
            </StackPanel>
        </Border>
    </Grid>
</Window>
