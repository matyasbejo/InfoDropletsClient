<Window x:Class="InfoDropletsClient.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"  
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:InfoDropletsClient"
        xmlns:map="clr-namespace:GMap.NET.WindowsPresentation;assembly=GMap.NET.WindowsPresentation"
        xmlns:vms="clr-namespace:InfoDroplets.Client.ViewModels"
        xmlns:converters="clr-namespace:InfoDroplets.Client.Converters"
        mc:Ignorable="d"
        Title="MainWindow" Height="675" Width="1200">
    
    <Window.DataContext>
        <vms:MainWindowViewModel/>
    </Window.DataContext>

        <Window.Resources>
        <Style TargetType="Button" x:Key="RCButton">
            <Setter Property="Height" Value="25"/>
            <Setter Property="Background" Value="#4CAF50"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#388E3C"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
        <converters:BooleanNegationConverter x:Key="BooleanNegationConverter"/>
        <converters:InstanceToBooleanConverter x:Key="InstanceToBooleanConverter"/>
    </Window.Resources>
    
    <Grid Background="#F2F2F2">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <!-- Top toolbar row -->
            <RowDefinition Height="1*"/>
            <!-- Main content row -->
            <RowDefinition Height="8*"/>
            <!-- Status row -->
            <RowDefinition Height="0.4*"/>
        </Grid.RowDefinitions>

        <Border x:Name="NavbarArea" Background="LightBlue" Margin="7" CornerRadius="10" Grid.Column="0" Grid.Row="0" Grid.ColumnSpan="2" VerticalAlignment="Stretch">
            <Grid Margin="3,2,0,2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Grid x:Name="grid_SerialSettingsGrid" VerticalAlignment="Stretch" Grid.Column="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="1*"/>
                        <RowDefinition Height="1*"/>
                    </Grid.RowDefinitions>

                    <Label x:Name="lb_SerialPort" Content="Serial port:" VerticalContentAlignment="Center" Grid.Row="0" Grid.Column="0"/>
                    <Label x:Name="lb_BaudRate" Content="Baud rate:" VerticalContentAlignment="Center" Grid.Row="1" Grid.Column="0"/>
                    <ComboBox x:Name="cb_PortOptions"  Width="110" Margin="1" Grid.Row="0" Grid.Column="1" ItemsSource="{Binding SerialWrapper.AvaliableSerialPorts}" SelectedItem="{Binding SelectedPort}" IsEnabled="{Binding SerialWrapper.IsOpen, Converter={StaticResource BooleanNegationConverter}}"/>
                    <ComboBox x:Name="cb_BaudRateOptions"  Width="110" Margin="1" Grid.Row="1" Grid.Column="1" ItemsSource="{Binding SerialWrapper.AvaliableBaudRates}" SelectedIndex="1" SelectedItem="{Binding SelectedBaudRate}" IsEnabled="{Binding SerialWrapper.IsOpen, Converter={StaticResource BooleanNegationConverter}}"/>
                    <Button x:Name="bt_StartCommunication" Content="Start" Width="80" Command="{Binding StartSerialCommand}" Margin="1" Grid.Row="0" Grid.RowSpan="2" Grid.Column="3"/>
                    <Button x:Name="bt_StopCommunication" Content="Stop" Width="80" Command="{Binding StopSerialCommand}" Margin="1" Grid.Row="0" Grid.RowSpan="2" Grid.Column="2"/>
                </Grid>
                <Grid x:Name="grid_DropletSelectorGrid" VerticalAlignment="Stretch" Grid.Column="2" Margin="0,8,10,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Label Content="Monitored droplet: " FontSize="16" VerticalContentAlignment="Center" Grid.Column="0"/>
                    <ComboBox x:Name="cb_AvaliableDroplets" FontSize="16" VerticalContentAlignment="Center" IsEnabled="{Binding SerialWrapper.IsOpen}" ItemsSource="{Binding AvaliableDropletIds}" SelectedIndex="0" SelectedItem="{Binding SelectedId}" Grid.Column="1"/>
                </Grid>
            </Grid>
        </Border>

        <Border x:Name="MapArea" Background="#E0E0E0" Margin="7" CornerRadius="10" Grid.Column="0" Grid.Row="1" Padding="3">
            <map:GMapControl x:Name="myMap" Position="{Binding MapCenterPos, Mode=OneWay}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" ClipToBounds="True"/>
        </Border>

        <Border x:Name="DetailsArea" Background="#D9D9D9" Margin="7" CornerRadius="10" Padding="10" Grid.Column="1" Grid.Row="1">
            <StackPanel>
                <TextBlock Text="{Binding SelectedDroplet.Id, StringFormat={}🎈 Droplet #{0},FallbackValue='Select a droplet to start'}" FontSize="18" FontWeight="Bold" Margin="0,0,0,15"/>

                <TextBlock Text="Altitude:" FontWeight="Bold"/>
                <TextBlock x:Name="tb_altitude" Text="{Binding SelectedDroplet.LastData.Elevation, StringFormat={}{0} m}" Margin="0,0,0,10"/>
                <TextBlock Text="Speed:" FontWeight="Bold"/>
                <TextBlock x:Name="tb_speed" Text="{Binding SelectedDroplet.SpeedKmH, StringFormat={}{0} km/h}" Margin="0,0,0,10"/>

                <TextBlock Text="Latitude:" FontWeight="Bold"/>
                <TextBlock x:Name="tb_latitude" Text="{Binding SelectedDroplet.LastData.Latitude}" Margin="0,0,0,10"/>

                <TextBlock Text="Longitude:" FontWeight="Bold"/>
                <TextBlock x:Name="tb_longitude" Text="{Binding SelectedDroplet.LastData.Longitude}" Margin="0,0,0,10"/>

                <TextBlock Text="Status:" FontWeight="Bold"/>
                <TextBlock x:Name="tb_status" Text="{Binding SelectedDroplet.MovementStatus}" Margin="0,0,0,10"/>

                <TextBlock Text="Distance:" FontWeight="Bold"/>
                <TextBlock x:Name="tb_distance" Text="{Binding SelectedDroplet.SpeedKmH, StringFormat={}{0} km}" Margin="0,0,0,10"/>

                <TextBlock Text="Last update:" FontWeight="Bold"/>
                <TextBlock x:Name="tb_lastUpdate" Text="{Binding SelectedDroplet.LastUpdated}" Foreground="Green" Margin="0,0,0,10"/>

                <Grid Background="DarkGray">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1*"/>
                        <ColumnDefinition Width="1*"/>
                        <ColumnDefinition Width="1*"/>
                    </Grid.ColumnDefinitions>

                    <Button Content="File version" Style="{StaticResource RCButton}" Margin="4" IsEnabled="{Binding SelectedDroplet, Converter={StaticResource InstanceToBooleanConverter}}" Grid.Column="0"/>
                    <Button Content="Ping" Style="{StaticResource RCButton}" Margin="4" IsEnabled="{Binding SelectedDroplet, Converter={StaticResource InstanceToBooleanConverter}}" Grid.Column="1">
                        
                    </Button>
                    <Button Content="Restart" Style="{StaticResource RCButton}" Margin="4" IsEnabled="{Binding SelectedDroplet, Converter={StaticResource InstanceToBooleanConverter}}" Grid.Column="2"/>
                </Grid>
            </StackPanel>
        </Border>
    </Grid>
</Window>
